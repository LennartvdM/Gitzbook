[build]
  command = "npm install && npm run build && cd examples/surface-preview && npm install && npm run build && cp -r dist ../../_book/surface-preview"
  publish = "_book"

[build.environment]
  NODE_VERSION = "18"
